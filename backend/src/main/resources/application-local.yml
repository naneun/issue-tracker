spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/testdb
    username: root
    password: test

  sql:
    init:
      mode: always

  redis:
    host: localhost
    port: 6379
